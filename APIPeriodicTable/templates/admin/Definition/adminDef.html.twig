<article class="adminDef">
    <h5>Définitions.</h5>
    <div class="my-2 groupCard">
        {% for Definition in Definitions %}
            <div class="card cardCat my-1 mx-1">
                <a href="{{ path('app_update_definition', {id: Definition.id}) }}" class="LinkAdminAtom">
                    <div class="card-body">
                        <div class="CardSymbole text-center">{{ Definition.name }}</div>
                    </div>
                </a>
            </div>
        {% endfor %}
        {% if is_granted('ROLE_SUPER_ADMIN')%}
        <div class="card cardCat my-1 mx-1">
            <a href="{{ path('app_create_definition') }}">
                <div class="card-body">
                    <div class="CardSymbole text-center">+</div>
                </div>
            </a>
        </div>
        {% endif %}
    </div>
</article>